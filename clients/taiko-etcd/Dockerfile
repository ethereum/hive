# Use the official etcd image from Docker Hub
FROM quay.io/coreos/etcd:v3.5.0

# Expose the ports used by etcd
EXPOSE 2379 2380

# Optionally, set environment variables or add additional configuration
# ENV ETCD_NAME my-etcd
# ENV ETCD_DATA_DIR /var/lib/etcd

# CMD is already set in the official image to run etcd
