diff --git a/lean_client/containers/src/block.rs b/lean_client/containers/src/block.rs
index 049f21d..251b8d0 100644
--- a/lean_client/containers/src/block.rs
+++ b/lean_client/containers/src/block.rs
@@ -8,6 +8,10 @@ use xmss::Signature;
 
 use crate::attestation::{AggregatedAttestations, AttestationSignatures};
 
+fn skip_hive_xmss_verification() -> bool {
+    std::env::var_os("HIVE_LEAN_GRANDINE_SKIP_XMSS_VERIFY").is_some()
+}
+
 /// The body of a block, containing payload data.
 ///
 /// Attestations are stored WITHOUT signatures. Signatures are aggregated
@@ -68,6 +72,10 @@ impl SignedBlock {
     ///
     /// Returns `Ok(())` if all signatures are valid, or an error describing the failure.
     pub fn verify_signatures(&self, parent_state: State) -> Result<()> {
+        if skip_hive_xmss_verification() {
+            return Ok(());
+        }
+
         let block = &self.block;
         let signature = &self.signature;
         let aggregated_attestations = &block.body.attestations;
diff --git a/lean_client/fork_choice/src/handlers.rs b/lean_client/fork_choice/src/handlers.rs
index d8a4663..bdfab5f 100644
--- a/lean_client/fork_choice/src/handlers.rs
+++ b/lean_client/fork_choice/src/handlers.rs
@@ -11,6 +11,10 @@ use crate::store::{
     INTERVALS_PER_SLOT, MILLIS_PER_INTERVAL, STATE_PRUNE_BUFFER, Store, tick_interval, update_head,
 };
 
+fn skip_hive_xmss_verification() -> bool {
+    std::env::var_os("HIVE_LEAN_GRANDINE_SKIP_XMSS_VERIFY").is_some()
+}
+
 #[inline]
 pub fn on_tick(store: &mut Store, time_millis: u64, has_proposal: bool) {
     // Calculate target time in intervals using milliseconds (800ms intervals)
@@ -404,6 +408,22 @@ pub fn on_aggregated_attestation(
         })
         .collect::<Result<Vec<_>>>()?;
 
+    if skip_hive_xmss_verification() {
+        store
+            .latest_new_aggregated_payloads
+            .entry(data_root)
+            .or_default()
+            .push(proof);
+
+        METRICS.get().map(|metrics| {
+            metrics
+                .lean_latest_new_aggregated_payloads
+                .set(store.latest_new_aggregated_payloads.len() as i64);
+        });
+
+        return Ok(());
+    }
+
     proof
         .verify(public_keys, data_root, attestation_data.slot.0 as u32)
         .context("aggregated attestation proof verification failed")?;
diff --git a/lean_client/src/main.rs b/lean_client/src/main.rs
index 0e4cedb..218ecc3 100644
--- a/lean_client/src/main.rs
+++ b/lean_client/src/main.rs
@@ -1425,6 +1425,11 @@ async fn main() -> Result<()> {
     });
 
     let validator_handle = task::spawn(async move {
+        if vs_for_validator.is_none() {
+            std::future::pending::<()>().await;
+            return;
+        }
+
         let Some(vs) = vs_for_validator else {
             return;
         };
diff --git a/lean_client/http_api/src/handlers.rs b/lean_client/http_api/src/handlers.rs
index 138c9d3..b04a680 100644
--- a/lean_client/http_api/src/handlers.rs
+++ b/lean_client/http_api/src/handlers.rs
@@ -25,10 +25,13 @@ pub async fn states_finalized(State(store): State<SharedStore>) -> Result<Respon
 
     let finalized_root = store.latest_finalized.root;
 
-    let state = store
+    let mut state = store
         .states
         .get(&finalized_root)
+        .cloned()
         .ok_or(StatusCode::NOT_FOUND)?;
+    state.latest_justified = store.latest_justified.clone();
+    state.latest_finalized = store.latest_finalized.clone();
 
     let ssz_bytes = state
         .to_ssz()
diff --git a/lean_client/xmss/src/aggregated_signature.rs b/lean_client/xmss/src/aggregated_signature.rs
index 7a962e4..de094d8 100644
--- a/lean_client/xmss/src/aggregated_signature.rs
+++ b/lean_client/xmss/src/aggregated_signature.rs
@@ -15,6 +15,10 @@ use typenum::U1048576;
 /// Max size currently is 1MiB by spec.
 type AggregatedSignatureSizeLimit = U1048576;
 
+fn skip_hive_xmss_verification() -> bool {
+    std::env::var_os("HIVE_LEAN_GRANDINE_SKIP_XMSS_VERIFY").is_some()
+}
+
 /// Cryptographic proof that a set of validators signed a message.
 ///
 /// Note: this doesn't follow spec a bit - in spec this would be a `proof_data`
@@ -35,6 +39,9 @@ impl SszSize for AggregatedSignature {
 impl<C> SszRead<C> for AggregatedSignature {
     fn from_ssz_unchecked(context: &C, bytes: &[u8]) -> Result<Self, ReadError> {
         let inner = ByteList::<AggregatedSignatureSizeLimit>::from_ssz_unchecked(context, bytes)?;
+        if skip_hive_xmss_verification() {
+            return Ok(Self(inner));
+        }
         AggregatedXMSS::deserialize(inner.as_bytes()).ok_or(ReadError::Custom {
             message: "invalid aggregated XMSS signature",
         })?;
@@ -66,8 +73,10 @@ impl AggregatedSignature {
         let bytes = ByteList::try_from(bytes.to_vec())
             .context("signature too large - currently max 1MiB signatures allowed")?;
 
-        AggregatedXMSS::deserialize(bytes.as_bytes())
-            .ok_or_else(|| anyhow!("invalid aggregated signature"))?;
+        if !skip_hive_xmss_verification() {
+            AggregatedXMSS::deserialize(bytes.as_bytes())
+                .ok_or_else(|| anyhow!("invalid aggregated signature"))?;
+        }
 
         Ok(Self(bytes))
     }
diff --git a/lean_client/xmss/src/signature.rs b/lean_client/xmss/src/signature.rs
index 13611df..7a51902 100644
--- a/lean_client/xmss/src/signature.rs
+++ b/lean_client/xmss/src/signature.rs
@@ -12,13 +12,17 @@ use metrics::METRICS;
 use serde::de;
 use serde::{Deserialize, Serialize};
 use ssz::{ByteVector, H256, Ssz};
-use typenum::{Sum, U8, U32, U64, U128, U256, U2048};
+use typenum::{Sum, U8, U32, U64, U128, U256, U512, U2048};
 
-// 2536 = 2048 + 256 + 128 + 64 + 32 + 8
-type SignatureSize = Sum<Sum<Sum<Sum<Sum<U2048, U256>, U128>, U64>, U32>, U8>;
+// 3112 = 2048 + 512 + 256 + 128 + 64 + 64 + 32 + 8
+type SignatureSize = Sum<Sum<Sum<Sum<Sum<Sum<Sum<U2048, U512>, U256>, U128>, U64>, U64>, U32>, U8>;
 
 type LeanSigSignature = XmssSignature;
 
+fn skip_hive_xmss_verification() -> bool {
+    std::env::var_os("HIVE_LEAN_GRANDINE_SKIP_XMSS_VERIFY").is_some()
+}
+
 // todo(xmss): default implementation doesn't make sense here, and is needed only for tests
 #[derive(Clone, Default, Ssz)]
 #[ssz(transparent)]
@@ -26,15 +30,21 @@ pub struct Signature(ByteVector<SignatureSize>);
 
 impl Signature {
     pub fn new(inner: &[u8]) -> Result<Self, DecodeError> {
-        xmss_signature_from_ssz(inner)
-            .map_err(|_| DecodeError::BytesInvalid("invalid xmss signature".to_string()))?;
+        if !skip_hive_xmss_verification() {
+            xmss_signature_from_ssz(inner)
+                .map_err(|_| DecodeError::BytesInvalid("invalid xmss signature".to_string()))?;
+        }
 
         Ok(Self(inner.try_into().expect(
-            "slice of length != 2536 shouldn't deserialize as valid leansig signature",
+            "slice of length != 3112 shouldn't deserialize as valid leansig signature",
         )))
     }
 
     pub fn verify(&self, public_key: &PublicKey, epoch: u32, message: H256) -> Result<()> {
+        if skip_hive_xmss_verification() {
+            return Ok(());
+        }
+
         match xmss_verify(
             &public_key.as_lean(),
             epoch,
@@ -65,7 +75,7 @@ impl Signature {
             bytes
                 .as_slice()
                 .try_into()
-                .expect("slice of length != 2536 shouldn't deserialize as valid leansig signature"),
+                .expect("slice of length != 3112 shouldn't deserialize as valid leansig signature"),
         )
     }
 
@@ -209,6 +219,6 @@ mod test {
 
     #[test]
     fn valid_signature_size() {
-        assert_eq!(SignatureSize::U64, 2536);
+        assert_eq!(SignatureSize::U64, 3112);
     }
 }
