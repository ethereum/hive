diff --git a/lean_client/http_api/src/handlers.rs b/lean_client/http_api/src/handlers.rs
index b55ac4c..b04a680 100644
--- a/lean_client/http_api/src/handlers.rs
+++ b/lean_client/http_api/src/handlers.rs
@@ -30,8 +30,11 @@ pub async fn states_finalized(State(store): State<SharedStore>) -> Result<Respon
     let finalized_root = store.latest_finalized.root;
 
-    let state = store
+    let mut state = store
         .states
         .get(&finalized_root)
+        .cloned()
         .ok_or(StatusCode::NOT_FOUND)?;
+    state.latest_justified = store.latest_justified.clone();
+    state.latest_finalized = store.latest_finalized.clone();
 
     let ssz_bytes = state
         .to_ssz()
diff --git a/lean_client/src/main.rs b/lean_client/src/main.rs
index 43463ec..77db75d 100644
--- a/lean_client/src/main.rs
+++ b/lean_client/src/main.rs
@@ -571,6 +571,8 @@ async fn main() -> Result<()> {
     let num_validators = anchor_state.validators.len_u64();
     info!(num_validators = num_validators, "Anchor state loaded");
 
+    let require_hive_hash_sig_keys = std::env::var_os("HIVE_LEAN_GRANDINE_REQUIRE_KEYS").is_some();
+
     let validator_service = if let (Some(node_id), Some(registry_path)) =
         (&args.node_id, &args.validator_registry_path)
     {
@@ -597,17 +599,35 @@ async fn main() -> Result<()> {
                                 Some(service)
                             }
                             Err(e) => {
-                                warn!(
-                                    "Failed to load XMSS keys: {}, falling back to zero signatures",
-                                    e
-                                );
-                                Some(ValidatorService::new_with_aggregator(
-                                    config,
-                                    num_validators,
-                                    args.is_aggregator,
-                                ))
+                                if require_hive_hash_sig_keys {
+                                    warn!(
+                                        node_id = %node_id,
+                                        indices = ?config.validator_indices,
+                                        keys_dir = ?keys_path,
+                                        "Failed to load XMSS keys under Hive: {e}; running in passive mode"
+                                    );
+                                    None
+                                } else {
+                                    warn!(
+                                        "Failed to load XMSS keys: {}, falling back to zero signatures",
+                                        e
+                                    );
+                                    Some(ValidatorService::new_with_aggregator(
+                                        config,
+                                        num_validators,
+                                        args.is_aggregator,
+                                    ))
+                                }
                             }
                         }
+                    } else if require_hive_hash_sig_keys {
+                        warn!(
+                            node_id = %node_id,
+                            indices = ?config.validator_indices,
+                            keys_dir = ?keys_path,
+                            "Hash-sig key directory not found under Hive; running in passive mode"
+                        );
+                        None
                     } else {
                         warn!(
                             "Hash-sig key directory not found: {:?}, using zero signatures",
@@ -619,6 +639,14 @@ async fn main() -> Result<()> {
                             args.is_aggregator,
                         ))
                     }
+                } else if require_hive_hash_sig_keys {
+                    info!(
+                        node_id = %node_id,
+                        indices = ?config.validator_indices,
+                        aggregator = args.is_aggregator,
+                        "Validator registry loaded without XMSS keys under Hive; running in passive mode"
+                    );
+                    None
                 } else {
                     info!(
                         node_id = %node_id,
@@ -1322,6 +1350,11 @@ async fn main() -> Result<()> {
     });
 
     let validator_handle = task::spawn(async move {
+        if vs_for_validator.is_none() {
+            std::future::pending::<()>().await;
+            return;
+        }
+
         let Some(vs) = vs_for_validator else {
             return;
         };
diff --git a/lean_client/containers/src/block.rs b/lean_client/containers/src/block.rs
index 7c8ff85..2473bb9 100644
--- a/lean_client/containers/src/block.rs
+++ b/lean_client/containers/src/block.rs
@@ -7,6 +7,10 @@ use xmss::Signature;
 
 use crate::attestation::{AggregatedAttestations, AttestationSignatures};
 
+fn skip_hive_xmss_verification() -> bool {
+    std::env::var_os("HIVE_LEAN_GRANDINE_SKIP_XMSS_VERIFY").is_some()
+}
+
 /// The body of a block, containing payload data.
 ///
 /// Attestations are stored WITHOUT signatures. Signatures are aggregated
@@ -123,6 +127,10 @@ impl SignedBlockWithAttestation {
     ///
     /// Returns `Ok(())` if all signatures are valid, or an error describing the failure.
     pub fn verify_signatures(&self, parent_state: State) -> Result<()> {
+        if skip_hive_xmss_verification() {
+            return Ok(());
+        }
+
         // Unpack the signed block components
         let block = &self.message.block;
         let signatures = &self.signature;
diff --git a/lean_client/fork_choice/src/handlers.rs b/lean_client/fork_choice/src/handlers.rs
index 5a0b24f..e0d778a 100644
--- a/lean_client/fork_choice/src/handlers.rs
+++ b/lean_client/fork_choice/src/handlers.rs
@@ -12,6 +12,10 @@ use crate::store::{
     INTERVALS_PER_SLOT, MILLIS_PER_INTERVAL, STATE_PRUNE_BUFFER, Store, tick_interval, update_head,
 };
 
+fn skip_hive_xmss_verification() -> bool {
+    std::env::var_os("HIVE_LEAN_GRANDINE_SKIP_XMSS_VERIFY").is_some()
+}
+
 #[inline]
 pub fn on_tick(store: &mut Store, time_millis: u64, has_proposal: bool) {
     // Calculate target time in intervals using milliseconds (devnet-3: 800ms intervals)
@@ -398,9 +402,11 @@ pub fn on_aggregated_attestation(
         })
         .collect::<Result<Vec<_>>>()?;
 
-    proof
-        .verify(public_keys, data_root, attestation_data.slot.0 as u32)
-        .context("aggregated attestation proof verification failed")?;
+    if !skip_hive_xmss_verification() {
+        proof
+            .verify(public_keys, data_root, attestation_data.slot.0 as u32)
+            .context("aggregated attestation proof verification failed")?;
+    }
 
     // Per leanSpec: Store the verified proof in latest_new_aggregated_payloads
     // Each participating validator gets an entry via their SignatureKey
diff --git a/lean_client/xmss/src/signature.rs b/lean_client/xmss/src/signature.rs
index 19b842e..5776526 100644
--- a/lean_client/xmss/src/signature.rs
+++ b/lean_client/xmss/src/signature.rs
@@ -21,6 +21,10 @@ type SignatureSize = U3112;
 
 type LeanSigSignature = <SIGTopLevelTargetSumLifetime32Dim64Base8 as SignatureScheme>::Signature;
 
+fn skip_hive_xmss_verification() -> bool {
+    std::env::var_os("HIVE_LEAN_GRANDINE_SKIP_XMSS_VERIFY").is_some()
+}
+
 // todo(xmss): default implementation doesn't make sense here, and is needed only for tests
 #[derive(Clone, Default, Ssz)]
 #[ssz(transparent)]
@@ -28,7 +32,9 @@ pub struct Signature(ByteVector<SignatureSize>);
 
 impl Signature {
     pub fn new(inner: &[u8]) -> Result<Self, DecodeError> {
-        LeanSigSignature::from_bytes(inner)?;
+        if !skip_hive_xmss_verification() {
+            LeanSigSignature::from_bytes(inner)?;
+        }
 
         Ok(Self(inner.try_into().expect(
             "slice of length != 3112 shouldn't deserialize as valid leansig signature",
@@ -36,6 +42,10 @@ impl Signature {
     }
 
     pub fn verify(&self, public_key: &PublicKey, epoch: u32, message: H256) -> Result<()> {
+        if skip_hive_xmss_verification() {
+            return Ok(());
+        }
+
         let is_valid = <SIGTopLevelTargetSumLifetime32Dim64Base8 as SignatureScheme>::verify(
             &public_key.as_lean(),
             epoch,
